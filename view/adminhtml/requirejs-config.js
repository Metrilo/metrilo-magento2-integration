/**
 * Add import js file to
 * Require JS mapping
 */
var config = {
    map: {
        '*': {
            metriloImport: 'Metrilo_Analytics/js/import'
        }
    },
    deps: [
        "jquery"
    ]
};